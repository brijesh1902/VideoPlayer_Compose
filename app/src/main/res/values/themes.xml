<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.VideoPlayer" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.VideoPlayer.Fullscreen" parent="Theme.VideoPlayer">
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_round</item>
        <item name="postSplashScreenTheme">@style/Theme.VideoPlayer.Fullscreen</item>
    </style>


    <style name="ThemeOverlay.VideoPlayer.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>